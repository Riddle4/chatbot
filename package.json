{
  "name": "chatbot-cmc-flamy",
  "version": "1.0.0",
  "private": true,
  "description": "Chatbot Flamy pour le Centre de Magie de la Cote",
  "scripts": {
    "start": "node server.js",
    "build:worker": "node scripts/build-worker.js",
    "deploy:worker": "npm run build:worker && wrangler deploy"
  },
  "engines": {
    "node": ">=20"
  },
  "devDependencies": {
    "wrangler": "^4.0.0"
  }
}
